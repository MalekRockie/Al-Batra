/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    output: 'export',
    basePath: '/Al-Batra',
    assetPrefix: '/Al-Batra/',
    images: {
    unoptimized: true,
  },

};

export default nextConfig;
